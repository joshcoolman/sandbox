{
	"name": "sandbox-chatroom-worker",
	"version": "0.0.0",
	"private": true,
	"description": "Cloudflare Worker + Durable Object backing the chatroom design experiment in /app/design-experiments/(experiments)/chatroom/",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"types": "wrangler types",
		"dry-run": "wrangler deploy --dry-run --outdir=.wrangler/dry-run"
	},
	"devDependencies": {
		"wrangler": "^4.85.0"
	}
}
