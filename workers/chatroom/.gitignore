# Wrangler local state (build cache, dev sockets, simulated DO storage)
.wrangler/

# Local dev secrets — wrangler dev auto-loads these. NEVER commit.
.dev.vars
.dev.vars.*

# Dependencies
node_modules/

# Note: worker-configuration.d.ts IS committed (regenerated by `wrangler types`
# when bindings change in wrangler.jsonc). Keeps `tsc` working out of the box.
